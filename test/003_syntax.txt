// Testing the parser

u8 main() {
  u8 one = 1;
  u8 two = 2;
  print(two);
  return one;
}

void print(u8 number) {
  // print out number
}

void printXTimes(u8 count) {
  while (count > 0) {
    print(count);
    count = count - 1;
  }
}
